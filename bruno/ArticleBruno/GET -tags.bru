meta {
  name: GET /tags
  type: http
  seq: 6
}

get {
  url: {{baseURL}}/tags
  body: none
  auth: inherit
}

tests {
  test("deve retornar um array e status 200", function() {
    expect(res.getStatus()).to.equal(200)
    expect(res.getBody()).to.be.an("array");
  })
}

settings {
  encodeUrl: true
  timeout: 0
}
